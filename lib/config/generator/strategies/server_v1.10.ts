import { ServerBaseStrategy } from './server_base';

/** v1.10 Server — baseline, all defaults from ServerBaseStrategy */
export class ServerV110Strategy extends ServerBaseStrategy {
    version = '1.10';
}

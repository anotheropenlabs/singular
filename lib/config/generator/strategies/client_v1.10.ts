import { ClientBaseStrategy } from './client_base';

/** v1.10 Client — baseline, all defaults from ClientBaseStrategy */
export class ClientV110Strategy extends ClientBaseStrategy {
    version = 'client-1.10';
}
